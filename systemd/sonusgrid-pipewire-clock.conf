# PipeWire systemd drop-in installed by SonusGrid.
# Allows clock_settime / adjtimex syscalls so Statime can steer the system
# clock from the same user session that PipeWire runs under.
[Service]
SystemCallFilter=@clock
